<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicinetrackerandreminder">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".MedicineTrackerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MedicineTracker">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication -->
        <activity android:name=".RegisterActivity" />

        <!-- Main app screens -->
        <activity android:name=".DashboardActivity" />
        <activity android:name=".AddMedicineActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".MyMedsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" />

        <!-- Broadcast receivers for alarms and actions -->
        <receiver
            android:name=".receivers.MedicineAlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".receivers.MedicineActionReceiver"
            android:exported="false" />

    </application>
</manifest>